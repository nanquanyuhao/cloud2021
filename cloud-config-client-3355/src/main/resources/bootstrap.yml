server:
  port: 3355

spring:
  application:
    name: config-client
  cloud:
    config:
      # 读取分支
      label: master
      name: config
      profile: dev
      uri: http://config-3344.com:3344
  # rabbitmq 相关配置
  rabbitmq:
    host: 192.168.235.104
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"